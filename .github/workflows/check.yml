name: Check

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v7
      - uses: purcell/setup-emacs@master
        with:
          version: 30.2
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install --yes hunspell-en-us shellcheck
      - name: Run quality suite
        # Keep Jinx's compiler and native libraries in the same Nix environment
        # as the Emacs installed by setup-emacs.
        run: nix-shell --packages enchant gcc hunspell pkg-config --run 'make check'
